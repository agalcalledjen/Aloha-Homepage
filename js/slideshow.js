$(document).ready(function () {
  // Flickity Carousel
  $('.main-carousel').flickity({
    // options
    cellAlign: 'left',
    contain: true,
    wrapAround: true,
    autoPlay: 4000,
    prevNextButtons: false,
  }); // end of Flickity carousel
}); // end of doc ready